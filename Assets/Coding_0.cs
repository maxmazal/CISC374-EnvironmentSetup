using UnityEngine;

public class Coding_0 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Why do computer scientists get confused between Halloween and Christmas?");
        Debug.Log("Because Oct 31 = Dec 25");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
